{
  "name": "first-lit",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "shell"
  ],
  "scripts": {
    "serve:shell": "cd shell && npm run dev",
    "serve:agent:restaurant": "cd ../server && uv run __main__.py",
    "demo:restaurant": "concurrently -k -n \"SHELL,REST\" -c \"magenta,blue\" \"npm run serve:shell\" \"npm run serve:agent:restaurant\""
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "shx": "^0.4.0"
  }
}
